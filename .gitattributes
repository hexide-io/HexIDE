# The VB6 conformance corpus is measured data, not source. Whitespace in it is LOAD-BEARING — trailing
# spaces and tabs after a line-continuation underscore are the whole point of several cases, and a
# right-trim or re-encode destroys the measurement without visibly changing the file. Keep git out of it.
corpus/** -text -diff
